import type React from "react"

// Deprecated: Legacy IAP SubScreen removed after migrating to Stripe.
// Keeping a no-op export temporarily to avoid broken imports during cleanup.

export const SubScreen: React.FC = () => {
  return null
}
